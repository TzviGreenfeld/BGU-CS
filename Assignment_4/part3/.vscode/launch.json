{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: test",
            "request": "launch",
            "command": "npm run test -- --verbose=true",
            "cwd": "${workspaceFolder}"
        }
    ]
}